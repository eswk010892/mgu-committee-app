# Leave these blank to run in demo mode (no sign-in, nothing shared, browser storage only).
# Fill them in to enable committee logins and the live public dashboard.
# Run supabase/schema.sql in your Supabase project first — without it nothing is protected.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
